{
  "name": "week-03-clean-architecture",
  "version": "1.0.0",
  "description": "Lab Test Module — Clean Architecture con Domain, Application, Infrastructure layers",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test --require ts-node/register tests/**/*.test.ts",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["clean-architecture", "ddd", "healthcare", "typescript"],
  "author": "Aldo",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
